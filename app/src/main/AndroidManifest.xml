<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ymejia.notes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pencil"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notes3">
        <activity
            android:name=".General.Users.ForgotActivity"
            android:exported="true" />
        <activity
            android:name=".General.Users.RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.Notes2" />
        <activity
            android:name=".General.HomeActivity"
            android:exported="true" />
        <activity
            android:name=".General.GeneralActivity"
            android:exported="true"
            android:theme="@style/Theme.Notes2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddNotesActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Notes3" /> <!-- The meta-data tag is required if you support API level 15 and lower -->
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".MainActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>